package test.acacian.singletonHeritance;

public class MySingleton<T> {
/*
 	//자바에서는 static 필드에서 제너릭 타입을 가질수 없다. (인스턴스 생성도)
	protected static T instance;
	
	private MySingleton(){
		
	}
	
	public static Object getInstance() {
		if(null == instance) {
			instance = new T();
		}	
		return instance;
	}
*/
}
